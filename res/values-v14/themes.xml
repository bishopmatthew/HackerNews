<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="V3.Light.NEW" parent="@style/V3.Light">
        <!-- General -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:selectableItemBackground">@drawable/btn_selectable_item</item>

        <!-- ActionBar -->
        <item name="android:actionDropDownStyle">@style/ActionBarSpinner</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_action_up</item>
        <item name="android:logo">@drawable/ic_logo</item>
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Pull To Refresh -->
        <item name="ptrHeaderStyle">@style/Widget.Custom.PtrHeader</item>

    </style>
</resources>